<%= form_for(@cliente_producto) do |f| %>
  <% if @cliente_producto.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@cliente_producto.errors.count, "error") %> prohibited this cliente_producto from being saved:</h2>

      <ul>
      <% @cliente_producto.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :cod %><br />
    <%= f.number_field :cod %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
