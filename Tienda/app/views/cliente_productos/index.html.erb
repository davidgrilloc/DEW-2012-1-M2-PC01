<h1>Listing cliente_productos</h1>

<table>
  <tr>
    <th>Cod</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @cliente_productos.each do |cliente_producto| %>
  <tr>
    <td><%= cliente_producto.cod %></td>
    <td><%= link_to 'Show', cliente_producto %></td>
    <td><%= link_to 'Edit', edit_cliente_producto_path(cliente_producto) %></td>
    <td><%= link_to 'Destroy', cliente_producto, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Cliente producto', new_cliente_producto_path %>
